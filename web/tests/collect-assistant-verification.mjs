// Round4: summarize current completed runs independently; never merges historical test outcomes.
import fs from 'node:fs';
const root='/opt/openbear/workspace/artifacts/clawguard-assistant-implementation/frontend-verification';
const tests=[],browser=[],observations=[];
for(const name of ['round4-targeted.json','round4-full-ci.json','round4-production-visual.json']){
 const report=JSON.parse(fs.readFileSync(`${root}/${name}`));
 function walk(s){for(const sp of s.specs??[])for(const t of sp.tests??[]){const r=t.results.at(-1);tests.push({report:name,title:sp.title,file:sp.file,project:t.projectName,status:r.status,attempts:t.results.length,errors:(r.errors??[]).map(e=>e.message.replace(/\x1b\[[0-9;]*m/g,'')),artifacts:(r.attachments??[]).filter(a=>a.path).map(a=>a.path)});for(const a of r.attachments??[])if(a.body&&a.contentType==='application/json'){const data=JSON.parse(Buffer.from(a.body,'base64').toString());const entry={report:name,test:sp.title,project:t.projectName,name:a.name,data};if(['assistant-browser-api-log','browser-and-api-log'].includes(a.name))browser.push(entry);else observations.push(entry);}}for(const a of s.suites??[])walk(a);}walk(report);
}
const count=ts=>({total:ts.length,passed:ts.filter(t=>t.status==='passed').length,failed:ts.filter(t=>!['passed','skipped'].includes(t.status)).length,skipped:ts.filter(t=>t.status==='skipped').length,retried:ts.filter(t=>t.attempts>1).length});
const full=tests.filter(t=>t.report==='round4-full-ci.json'),targeted=tests.filter(t=>t.report==='round4-targeted.json'),visual=tests.filter(t=>t.report==='round4-production-visual.json');
function unexpected(d){return d.console.filter(m=>typeof m==='string'?!/^Failed to load resource: the server responded with a status of (401|409|500)/.test(m):!(m.text.startsWith('Failed to load resource: the server responded with a status of ')&&d.injected.some(i=>m.text.includes(String(i.status))&&m.location.url.endsWith(i.path))));}
const diagnostics=bs=>({pageErrors:bs.flatMap(b=>b.data.errors).length,unmappedApi:bs.flatMap(b=>b.data.unknown).length,unexpectedConsoleErrors:bs.flatMap(b=>unexpected(b.data)).length,injectedHttpConsoleErrors:bs.flatMap(b=>b.data.console).length});
const sources=JSON.parse(fs.readFileSync(`${root}/source-preservation.json`));const lifecycle=JSON.parse(fs.readFileSync(`${root}/lifecycle.json`));
const definitions=[
 ['FE-DRAFT-001',t=>/saving.*second|second.*keyboard|R3 DRAFT/.test(t),'Native fieldset disabled blocks keyboard second edits; original snapshot survives; 500 retains draft and re-enables editing.'],
 ['FE-RACE-002',t=>/out of order|R3 RACE/.test(t),'Late memory/history/detail/version responses cannot replace newer query/object; closed/reopened dialog and cross-group pending request remain isolated.'],
 ['FE-MODAL-003',t=>/memory saving Esc|R3 DRAFT memory/.test(t),'Saving Escape gives no discard confirmation or close; 500 keeps original editable memory draft.'],
 ['FE-FOCUS-004',t=>/R3 FOCUS|memory modal keyboard focus/.test(t),'Create/edit/source direct trigger and Source -> administrator correction -> close restore correct surviving trigger; initial focus and Tab/Shift+Tab loops verified.'],
 ['FE-TOOLS-005',t=>/pool registry add remove|R3 TOOLS/.test(t),'Chat excludes pure text endpoints; invalid existing reference is natively disabled; chat backups filter text endpoints; learning still permits text registry models.']
];
const findings=definitions.map(([id,predicate,evidence])=>{const ts=full.filter(t=>predicate(t.title));return {id,status:ts.length&&ts.every(t=>t.status==='passed')?'CLOSED_PASS':'OPEN',evidence,counts:count(ts),tests:ts.map(t=>({title:t.title,project:t.project,report:t.report}))};});
const screenshotInfo=[['assistant-overview.png','emerald','overview'],['assistant-memory.png','ocean','memory'],['assistant-pool-graphite.png','graphite','pool'],['assistant-mobile.png','emerald','overview']].map(([file,theme,tab])=>{const b=fs.readFileSync(`${root}/screenshots/${file}`);return {path:`${root}/screenshots/${file}`,width:b.readUInt32BE(16),height:b.readUInt32BE(20),theme,tab,route:'/groups/-1001/assistant',realCurrentNextProductionBuild:true,fixture:true,online:false,preconditions:['correct route','expected API fixture DTO loaded','theme asserted','no unexpected modal']};});
const diag=diagnostics(browser);const pass=tests.every(t=>t.status==='passed'&&t.attempts===1)&&sources.productionUnchanged&&sources.legacyTestsUnchanged&&lifecycle.postflight.every(p=>p.free)&&!diag.pageErrors&&!diag.unmappedApi&&!diag.unexpectedConsoleErrors;
const command=(text,log,report)=>({command:text,cwd:'/opt/openbear/workspace/clawguard-assistant/web',exitCode:0,log,...(report?{report}:{})});
const result={status:pass?'PASS':'FAIL',round:4,workspace:'/opt/openbear/workspace/clawguard-assistant',branch:'feat/group-assistant',summary:'本轮一次默认完整CI316/316通过（旧154+助手162）；production1440/390定向6/6、视觉14/14通过。已知五类缺陷全部关闭。无历史结果合并、skip或retry。',commands:[command('npm run lint','round4-lint.log'),command('npm run build','round4-build.log'),command(`PLAYWRIGHT_JSON_OUTPUT_NAME=${root}/round4-targeted.json npx playwright test -c tests/assistant-verification.config.ts assistant-round3.spec.ts --grep='source to correction|invalid existing reference|strict mock selfcheck' --reporter=list,json --output=${root}/round4-targeted-test-results`,'round4-targeted.log','round4-targeted.json'),command(`PLAYWRIGHT_JSON_OUTPUT_NAME=${root}/round4-full-ci.json npm run test:e2e -- --workers=2 --reporter=list,json --output=${root}/round4-full-ci-test-results`,'round4-full-ci.log','round4-full-ci.json'),command(`PLAYWRIGHT_JSON_OUTPUT_NAME=${root}/round4-production-visual.json npx playwright test -c tests/assistant-verification.config.ts assistant-visual.spec.ts --reporter=list,json --output=${root}/round4-production-visual-test-results`,'round4-production-visual.log','round4-production-visual.json'),command('npm run lint (final test edits)','round4-lint-final.log')],counts:{fullDefaultCI:count(full),legacy:count(full.filter(t=>!t.file.startsWith('assistant'))),assistant:count(full.filter(t=>t.file.startsWith('assistant'))),productionTargeted:count(targeted),productionVisual:count(visual)},findings,openProductDefects:[],testCorrections:{status:'CLOSED',note:'Round3 option.toBeDisabled compatibility error replaced before this run with native JS disabled property + HTML disabled attribute assertions; current targeted and full runs PASS. No assertion weakened. Added Tab loop inside source-to-editor chain this round. Legacy154 untouched.'},apiEvidence:{status:'PASS_STRICT_MOCK',tests:full.filter(t=>/R3 API|strict mock selfcheck|conflict.*correct|memory.*exact safe/.test(t.title)).map(t=>({title:t.title,project:t.project,status:t.status})),verified:['Conflict accept first GETs current target version9 then exact accept:true + expected_memory_version9 + resolution_mode admin_explicit_correction; reject only accept:false.','Version changed to10 between GET/POST produces409, refresh and explicit no-success notice, no overwrite.','Memory create canonical scope and admin_base; update CAS with no forged operator/authority/source IDs/hash/source_type.','Canonical/alias source types and source_chat_id/message IDs/currently_verified displayed readonly.','Stored Telegram source invalid PUT409 retains full editable draft; versions404 no fake chain; invalid valid_scope causes no write.','Strict mock selfchecks reject POST/PUT source_message_id400, forged pinned source400, missing/foreign versions404 and legacy bare accept:true400.'],limit:'Browser+route mock only. Negative selfcheck validates the mock contract, not actual backend enforcement/transactions/source hashes.'},visualEvidence:{tests:count(visual),currentProductionScreenshots:observations.filter(o=>o.report==='round4-production-visual.json'&&o.name==='screenshot-ready').length,viewports:[{width:1440,height:1000},{width:390,height:844}],themes:['emerald','ocean','graphite'],assertions:['All five assistant tabs no document/body horizontal overflow','Wide tables scroll inside their container','Input contrast at least4.5 and theme switch preserves settings and memory modal text','Mini34px bottom safearea simulated; mobile save button44px and hit/nav reachable','Screenshot route/data/theme/no-modal asserted before capture']},screenshots:screenshotInfo,diagnostics:{allCurrentRuns:diag,byReport:Object.fromEntries(['round4-targeted.json','round4-full-ci.json','round4-production-visual.json'].map(n=>[n,diagnostics(browser.filter(b=>b.report===n))])),logs:['browser-logs.json','observations.json'],external:'Google font/Telegram SDK/widget requests intercepted before network. No production/Telegram/model service called.'},sourcePreservation:sources,lifecycle,archives:['archive-round2/','archive-round3/'],boundaries:{productionSourceEdited:false,legacySpecsEdited:false,newDependencyVersions:false,backendStarted:false,goOrPGTests:false,realSecretsRead:false,commitPushDeploy:false},notCovered:['Real backend browser integration; actual JWT scope/CAS transactions/source-hash/Telegram/model execution remains separate package.','Physical Telegram Mini App or OS safe-area measurement; SDK and34px safearea are test-only mocks.','Firefox/WebKit and human pixel aesthetic review; screenshots, DOM geometry/contrast/hit/focus are verified.','Production packaging/deployment: next start served current built app with standalone advisory; no standalone deployment claim.'],controllerNext:'No frontend defect remains open within this verification package. Integrate current artifacts; real backend browser integration remains separately authorized.',tests};
fs.writeFileSync(`${root}/results.json`,JSON.stringify(result,null,2)+'\n');fs.writeFileSync(`${root}/browser-logs.json`,JSON.stringify(browser,null,2)+'\n');fs.writeFileSync(`${root}/observations.json`,JSON.stringify(observations,null,2)+'\n');console.log(JSON.stringify({status:result.status,counts:result.counts,findings:findings.map(f=>({id:f.id,status:f.status,counts:f.counts})),diagnostics:result.diagnostics,screenshots:screenshotInfo},null,2));
