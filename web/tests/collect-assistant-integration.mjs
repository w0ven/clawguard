// Analyze only completed integration artifacts; never fabricates backend responses or runs services.
import fs from 'node:fs';
const root='/opt/openbear/workspace/artifacts/clawguard-assistant-implementation/integration-verification';
const read=n=>JSON.parse(fs.readFileSync(`${root}/${n}`));
const run=read('runner-result.json'),lifecycle=read('lifecycle.json'),source=read('source-preservation.json');
const report=fs.existsSync(`${root}/playwright.json`)?read('playwright.json'):null;
const tests=[];function walk(s){for(const spec of s.specs??[])for(const t of spec.tests??[]){const r=t.results.at(-1);tests.push({title:spec.title,file:spec.file,status:r.status,attempts:t.results.length,errors:(r.errors??[]).map(e=>e.message.replace(/\x1b\[[0-9;]*m/g,'')),attachments:(r.attachments??[]).map(a=>({name:a.name,path:a.path,body:a.contentType==='application/json'&&a.body?JSON.parse(Buffer.from(a.body,'base64').toString()):undefined}))});}for(const child of s.suites??[])walk(child);}if(report)walk(report);
const snapshots=Object.fromEntries(fs.readdirSync(root).filter(n=>n.startsWith('db-')&&n.endsWith('.json')).map(n=>[n,read(n)]));
const journals=['desktop','mobile'].filter(n=>fs.existsSync(`${root}/${n}-requests.json`)).map(n=>({viewport:n,...read(`${n}-requests.json`)}));
const unexpectedConsole=journals.flatMap(j=>j.console.filter(e=>!/^Failed to load resource: the server responded with a status of (401|403|404|409)/.test(e.text)));
const api=journals.flatMap(j=>j.api);const initial=snapshots['db-initial.json'],afterGet=snapshots['db-after-default-get.json'],settings=snapshots['db-after-settings.json'],pool=snapshots['db-after-pool.json'],cas=snapshots['db-after-cas409.json'],final=snapshots['db-mobile-final.json'];
const same=(a,b)=>JSON.stringify(a)===JSON.stringify(b);
const evidence={defaultReadNoPolicyOrPoolWrite:Boolean(initial&&afterGet&&afterGet.policies.length===0&&afterGet.pools.length===0&&afterGet.memories.length===0&&afterGet.messages.length===0),poolDidNotChangePolicy:Boolean(settings&&pool&&same(settings.policies,pool.policies)),oldModerationConfigurationUnchanged:Boolean(initial&&Object.values(snapshots).every(s=>same(s.groups,initial.groups)&&same(s.global_config,initial.global_config))),realCAS409:api.filter(r=>r.status===409).map(r=>({method:r.method,path:r.url,request:JSON.parse(r.request),response:r.body})),secondClientPersisted:cas?.policies.find(p=>p.chat_id===-88001)?.system_prompt==='Integration second client committed',adminAttribution:final?.memories.filter(m=>m.source_type==='admin_conflict_accept').map(m=>({id:m.id,version:m.version,authority_level:m.authority_level,source_type:m.source_type,source_operator_id:m.source_operator_id,source_operator_name:m.source_operator_name,source_message_id:m.source_message_id,source_chat_id:m.source_chat_id})),conflicts:final?.conflicts.map(c=>({id:c.id,status:c.status,resolved_by:c.resolved_by})),forgotten:final?.memories.filter(m=>!m.active).map(m=>({id:m.id,active:m.active,forgotten_by:m.forgotten_by,version:m.version})),scopedJWT403:tests.flatMap(t=>t.attachments).find(a=>a.name==='real-scope-403')?.body,foreignVersions404:tests.flatMap(t=>t.attachments).find(a=>a.name==='real-foreign-404')?.body,snapshots:Object.keys(snapshots)};
const screenshots=fs.existsSync(`${root}/screenshots`)?fs.readdirSync(`${root}/screenshots`).filter(n=>n.endsWith('.png')).map(n=>{const b=fs.readFileSync(`${root}/screenshots/${n}`);return {file:`screenshots/${n}`,width:b.readUInt32BE(16),height:b.readUInt32BE(20),realNextGoPostgres:true,apiFixtureResponses:false,seededNonProductionData:true};}):[];
const pass=run.exit_code===0&&tests.length===2&&tests.every(t=>t.status==='passed'&&t.attempts===1)&&source.unchanged&&lifecycle.remaining_containers.length===0&&lifecycle.ports.every(p=>p.free)&&evidence.defaultReadNoPolicyOrPoolWrite&&evidence.poolDidNotChangePolicy&&evidence.oldModerationConfigurationUnchanged&&evidence.secondClientPersisted&&journals.every(j=>j.js.length===0&&j.assistantResponsesMocked===0)&&unexpectedConsole.length===0;
const result={status:pass?'PASS':'FAIL',phase:'REAL_BROWSER_GO_POSTGRES_INTEGRATION',workspace:'/opt/openbear/workspace/clawguard-assistant',topology:{authoritative:'Repository Caddyfile routes same-origin /api/* to Go and pages/assets to Next; web has no Next API proxy.',actual:'Browser loads current production Next through test-local same-origin reverse proxy; assistant fetches enter actual Go NewServer HTTP handler, JWT/admin/CSRF middleware and real PostgreSQL.',difference:'Test-only local ingress reproduces Caddy routing; no production API route or auth bypass added. Do not describe this as Next server internally forwarding API requests.'},counts:{tests:tests.length,passed:tests.filter(t=>t.status==='passed').length,failed:tests.filter(t=>t.status!=='passed').length,skipped:report?.stats?.skipped??null,retried:tests.filter(t=>t.attempts>1).length,pageAPIResponses:api.length},coverage:['Desktop1440 complete defaults/settings/pool/memory/conflicts/concurrentCAS/forget/history/status/tools/scope flow','390px settings save+reload and memory create/edit real persistence','Actual migrations1..32 in disposable Postgres16 and actual DB-backed provider/model registries; no provider calls','Actual signAdminJWT and DB admin scopes through NewServer; cookie+CSRF protected browser writes; second scoped authenticated client produces real409','No assistant route fulfill or synthetic service DTO; API payload/status errors and before/after database snapshots retained'],evidence,diagnostics:{uncaughtJS:journals.flatMap(j=>j.js).length,unexpectedConsole:unexpectedConsole,expectedConsole409:journals.flatMap(j=>j.console).filter(e=>e.text.includes('409')).length,externalBrowserResources:'Only fonts/Telegram SDK/widget were intercepted before network.',goOutbound:'All Go default outbound blocked except constructor getMe local transport stub; launcher PASS asserts zero other outbound attempts.'},screenshots,commands:read('commands.json'),launcher:{file:'bot/internal/api/group_assistant_browser_integration_test.go',default:'Actual no-env invocation SKIP in launcher-default-skip.log; this SKIP is inertness verification, not integration success.',enabled:'Actual local-only invocation PASS with goose migration32, real NewServer+DB+miniredis; go-server.log.',session:'Temporary fixture JWT/CSRF/control secrets in private runtime file, removed after test. No real Telegram login exercised.'},sourcePreservation:source,lifecycle,changedFiles:['bot/internal/api/group_assistant_browser_integration_test.go','web/tests/assistant-integration.config.ts','web/tests/integration/assistant-real-api.spec.ts','web/tests/run-assistant-integration.py','web/tests/collect-assistant-integration.mjs'],notRun:['Real Telegram login/client/poll/webhook/delivery','Real provider calls, dispatch overflow/tool execution/SSRF/security matrices (owned by backend verifier)','Physical phones, full visual matrix, repeated316mock regression','Deployed Caddy binary/TLS/standalone packaging; this is authorized local ingress with production route topology'],boundaries:{productionChanged:false,existingDBOrRedisUsed:false,dependenciesChanged:false,realKeysRead:false,commitPushDeploy:false},tests};
fs.writeFileSync(`${root}/results.json`,JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({status:result.status,counts:result.counts,evidence:result.evidence,diagnostics:result.diagnostics,screenshots:result.screenshots},null,2));
