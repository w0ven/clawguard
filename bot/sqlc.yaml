version: "2"
sql:
  - engine: postgresql
    queries:
      - internal/store/queries
    schema:
      - migrations
    gen:
      go:
        package: store
        out: internal/store
        sql_package: pgx/v5
        emit_json_tags: true
        emit_interface: false
